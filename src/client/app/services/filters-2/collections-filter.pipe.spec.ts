import { CollectionsFilterPipe } from './collections-filter.pipe';

describe('CollectionsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CollectionsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
